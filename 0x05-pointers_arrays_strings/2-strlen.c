#include "main.h"
#include <stdio.h>

/**
 * _strlen - function to return the length of a string
 *
 * @s: given string
 *
 * Return: length of the string
 */

int _strlen(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		i++;
	}

	return (i)
}
